# Modules

This folder indexes module-level documentation.

The first planned module is [Identity](identity.md), which owns local user
identity translation and application-owned authorization records.

Product-specific modules should be added only when a concrete product needs
them.
