# Local Services

The template's local platform is expected to include PostgreSQL, Redis,
Keycloak, and Mailpit once orchestration is introduced.

These services are not wired in Gate 1. Future gates should document startup
commands, ports, data persistence, and reset behavior here.
