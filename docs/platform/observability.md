# Observability

The backend target uses OpenTelemetry through Aspire service defaults when the
server scaffold is introduced.

Future gates should document logging, metrics, tracing, local dashboards, and
the boundaries between template defaults and product-specific telemetry.
